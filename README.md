# これなに

ひなビタ♪ Facebook ページの投稿を蒐集するやつ

# 環境

Linux 前提

# 使い方

```
$ ./startup.sh
$ ./run.sh
```

これで，`hinabitter.txt` というテキストファイルが得られます。
`hinabitter.txt` は青空文庫形式のテキストです。
また，このレポジトリの `photos/` ディレクトリに取得した画像が配置されます。

